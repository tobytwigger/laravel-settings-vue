const getAllSettings = () => {
    return reactive({
        // get all settings
    })
}

const getValue = (key) => {
    // Get value from a setting
}

const setValue = (key) => {
    // Use the axios instance and return the promise
}
